import { MedSearchPipe } from './med-search.pipe';

describe('MedSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MedSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
